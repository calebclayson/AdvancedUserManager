extends layout

block content
    h1 Found
        h3 Email: #{email}
        h3 Name: #{name}
        h3 ID: #{id}
        h3 Age: #{age}
        p
            a(href="/users/all/default") List of All Users